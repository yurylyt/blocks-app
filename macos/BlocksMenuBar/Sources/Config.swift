import Foundation

enum BlocksConfig {
    static let baseURL = URL(string: "https://blocks.smerekalabs.com")!
    static let urlScheme = "blocks-menubar"
    static let cookieName = "nuxt-session"
}
